# 网页自学计划

> 项目：豪士面包网站（基于现有 `index.html + 原生 JS 模块`）
>
> 学习原则：**按底层逻辑优先级**，每个知识点都绑定现有站点功能改造，避免脱离项目空学。

---

## 一、五大板块目标水平（统一按 L0→L4）

### 1) HTML/CSS 盒模型与布局（最高优先级）
**目标到 L4：**
- 能快速读懂任意页面结构，不靠“改一行看一眼”试错
- 能独立重构一个 section（Hero / Products / Footer 任一）并保持视觉一致
- 能稳定处理定位、层级、溢出、响应式错位问题

---

### 2) CSS 体系化（变量/层级/响应式/动效）
**目标到 L4：**
- 能建立可维护样式系统（design tokens、组件样式、状态样式）
- 能控制样式复杂度，避免“越改越乱”
- 能独立实现主题切换与统一动效规范

---

### 3) JS 核心（事件/状态/DOM）
**目标到 L4：**
- 能把交互拆解为：事件输入 → 状态变更 → UI 渲染
- 能写出低耦合的交互逻辑，减少全局污染和隐式依赖
- 能独立实现中等复杂交互（滚动高亮、联动切换、状态同步）

---

### 4) JS 进阶（异步/并发/资源调度）
**目标到 L4：**
- 熟练 Promise、async/await、并发限制、超时与重试
- 能设计资源加载优先级与失败降级策略
- 能定位和优化首屏加载慢、交互阻塞、资源失败等问题

---

### 5) 图形与性能（动画渲染/降级策略）
**目标到 L4：**
- 理解 `requestAnimationFrame` 渲染循环与粒子系统更新逻辑
- 能用设备能力和实时表现做动态降级
- 能在“视觉效果 / 交互流畅 / 资源消耗”之间做工程化平衡

---

## 二、五大板块详细任务计划（递进式）

## 板块1：HTML/CSS 盒模型与布局（地基）
**核心目标：看懂并能重构页面结构，不靠试错。**

### Phase A：读懂结构（L0→L1）
1. **页面骨架图**
   - 画出 `nav / hero / story / products / footer` 结构树
   - 标注每个容器的语义职责与父子关系
2. **盒模型审计**
   - 抽查至少 15 个关键元素
   - 记录 `display / box-sizing / margin / padding / width/height`
3. **层级与定位地图**
   - 整理 `position + z-index` 关系
   - 特别标记固定层与特效层（如 cursor/light/fx canvas）

### Phase B：可控修改布局（L1→L2）
4. **Hero 区块等价重写**
   - 视觉不变，重写布局规则（如 grid 对齐策略）
5. **间距系统统一**
   - 将零散间距收敛为统一节奏（如 4/8/16/24）
6. **修复 3 个真实布局问题**
   - 每个问题输出：现象 → 根因 → 修复方法

### Phase C：响应式重构（L2→L3）
7. **建立断点策略（手机/平板/桌面）**
   - 每档明确：排版、字号、间距、布局变化
8. **Hero 双栏改单栏（移动端）**
   - 保证阅读顺序、CTA 优先级、无横向滚动
9. **固定元素适配**
   - 处理 nav/progress/cursor 在小屏冲突

### Phase D：独立重构挑战（L3→L4）
10. **Products 区块独立重构**
    - 不参考原写法，仅基于视觉目标重建布局
11. **预判式改动训练**
    - 每次改动前先写预期，连续 10 次至少 8 次命中
12. **60 分钟限时复刻**
    - 限时重建一个完整 section + 3 档响应式

**板块1达标标准：**
- 能解释关键布局决策
- 能快速定位错位根因
- 能在不试错的前提下完成 section 重构

---

## 板块2：CSS 体系化（可维护样式系统）
**核心目标：让样式可复用、可扩展、可维护。**

### Phase A：样式资产盘点（L0→L1）
1. **Design Tokens 清点**
   - 颜色、字体、字号、间距、圆角、阴影、过渡统一登记
2. **重复样式识别**
   - 找出按钮、标题、卡片、文本的重复声明
3. **状态样式扫描**
   - 汇总 hover/focus/active/disabled 的一致性问题

### Phase B：建立样式规范（L1→L2）
4. **变量规范化**
   - 补齐缺失 token，清理硬编码颜色/尺寸
5. **组件样式分层**
   - 区分基础类、组件类、状态类（避免互相覆盖）
6. **统一交互动效**
   - 统一 transition duration/easing，减少动画割裂

### Phase C：主题与响应式一致性（L2→L3）
7. **实现主题切换（浅色/深色）**
   - 基于 CSS 变量切换，不重写组件结构
8. **响应式字体和间距策略**
   - 统一 clamp/断点策略
9. **可访问性增强**
   - 焦点可见、对比度可读、动效可降级

### Phase D：系统化验收（L3→L4）
10. **重构 2 个复杂模块样式**
    - 要求代码量下降且可读性提升
11. **样式复杂度清理**
    - 减少覆盖链和 !important 依赖
12. **形成 CSS 设计规范清单**
    - 未来新增 UI 必须按规范接入

**板块2达标标准：**
- 新增一个模块时可复用现有体系
- 全站样式修改不引发连锁失控

---

## 板块3：JS 核心（事件/状态/DOM）
**核心目标：构建稳定交互逻辑，不靠“堆事件监听”。**

### Phase A：读懂交互流（L0→L1）
1. **交互地图绘制**
   - 列出所有用户输入：click/hover/scroll/keyboard
2. **状态清点**
   - 区分持久状态、临时状态、UI派生状态
3. **DOM 更新路径梳理**
   - 每个交互对应到具体 DOM 更新点

### Phase B：重构单一交互（L1→L2）
4. **声音开关重构**
   - 事件、状态、UI更新分离
5. **导航高亮实现**
   - 滚动驱动当前 section 状态
6. **事件委托改造**
   - 减少重复绑定，提高可维护性

### Phase C：多交互联动（L2→L3）
7. **模块通信规范**
   - 统一模块对外 API 和触发方式
8. **状态同步机制**
   - 避免多个模块维护同一状态副本
9. **异常流程处理**
   - 用户快速连点、滚动跳跃等边界场景稳定

### Phase D：结构化交互系统（L3→L4）
10. **抽象通用交互模式**
    - 如 toggle、active-item、section-observer
11. **清理全局污染**
    - 缩减 window 级别共享变量
12. **交互回归验证**
    - 覆盖核心路径与边缘路径

**板块3达标标准：**
- 能清晰讲出任一交互链路
- 新增交互不会破坏现有逻辑

---

## 板块4：JS 进阶（异步/并发/资源调度）
**核心目标：掌握真实工程中的加载控制能力。**

### Phase A：异步能力打底（L0→L1）
1. **Promise/async 流程图**
   - 将当前资源加载流程画成时序图
2. **错误链路梳理**
   - 区分网络失败、超时、资源缺失、解码失败
3. **可观测指标定义**
   - 定义首屏可交互、关键资源完成、总加载耗时

### Phase B：并发与优先级（L1→L2）
4. **并发队列验证**
   - 验证并发上限生效并记录性能差异
5. **资源分级重构**
   - critical / interactive / secondary 再细化
6. **重试策略优化**
   - 区分可重试与不可重试错误

### Phase C：稳定性与降级（L2→L3）
7. **超时策略分资源类型设置**
   - 图片、视频、音频采用不同阈值
8. **失败回退方案完善**
   - fallback 资源与占位逻辑
9. **慢网模拟测试**
   - 在 3G/高延迟下验证体验

### Phase D：性能闭环（L3→L4）
10. **打点与日志体系**
    - 输出关键阶段耗时与失败率
11. **首屏链路精简**
    - 推迟非关键资源
12. **加载策略复盘**
    - 形成“调度策略说明 + 验证结果”

**板块4达标标准：**
- 能独立设计并落地资源调度方案
- 能用数据解释优化前后差异

---

## 板块5：图形与性能（动画渲染/降级）
**核心目标：效果可控、性能可控、体验可控。**

### Phase A：渲染原理理解（L0→L1）
1. **渲染循环解构**
   - 读懂 `requestAnimationFrame` 帧逻辑
2. **粒子生命周期梳理**
   - 生成、更新、衰减、移除流程
3. **配置与渲染映射**
   - FX 配置项如何影响实际画面

### Phase B：参数调优能力（L1→L2）
4. **单参数实验**
   - 改 count/opactiy/lifetime 观察差异
5. **分场景参数集**
   - Story / Products / Footer 形成各自最佳值
6. **动效层级协调**
   - 避免多个特效叠加导致视觉噪声

### Phase C：性能治理（L2→L3）
7. **FPS 与资源占用监控**
   - 记录不同设备表现
8. **动态降级策略实现**
   - 低性能设备自动降粒子/降频/关次级效果
9. **减少主线程阻塞**
   - 清理高频不必要计算

### Phase D：工程化验收（L3→L4）
10. **全链路压力测试**
    - 滚动 + hover + 粒子同时触发
11. **性能预算设定**
    - 设定可接受 FPS 与加载开销
12. **最终动效方案定稿**
    - 输出效果层级与降级决策表

**板块5达标标准：**
- 动效在主流设备稳定流畅
- 能解释每项降级策略的触发条件和收益

---

## 三、推荐执行节奏（8~10 周）

- **第1-2周：板块1（布局地基）**
- **第3-4周：板块2（CSS体系化）**
- **第5-6周：板块3（JS核心交互）**
- **第7周：板块4（异步调度）**
- **第8周：板块5（渲染性能）**
- **第9-10周（可选）：综合重构与作品打磨**

每日建议：
- 20 分钟：原理学习
- 40~60 分钟：项目实操改造
- 10 分钟：复盘（问题、根因、改进）

---

## 四、阶段性验收方式

每完成一个板块，做一次 60~90 分钟限时任务：
- 不看旧代码，从目标效果反推实现
- 验证：结构清晰、行为稳定、可响应式适配、可解释关键决策

当 5 个板块全部验收通过，你将具备：
- 面向真实项目的前端开发能力（非教程式）
- 能独立迭代你的网站功能与体验
- 能持续优化结构、交互和性能，而不是仅“做出效果”
