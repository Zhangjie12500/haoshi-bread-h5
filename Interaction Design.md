# Interaction Design

> 方案：**烘焙指挥台（方案一）**  
> 目标：把 Factory 页面从“数据看板”升级成“可操控的工艺舞台”。  
> 主文件：`D:\Cursor Demo\haoshi-bread\index.html`

---

## 给 Cursor 的执行指令（可直接粘贴）

你现在是资深前端交互设计工程师。请在 `D:\Cursor Demo\haoshi-bread\index.html` 中，把 Factory 区域重构为“烘焙指挥台”交互（保留现有视觉风格：深棕+暖金+健康绿），并满足以下要求。

### 一、交互设计目标
把用户操作映射为“真实工艺控制”：
- 长按 = 升温控火候
- 横向划动/移动 = 调速
- 点击四卡 = 切换工艺模式

交互必须让用户立即感知：
- 我做了操作
- 产线立刻响应
- 面包状态发生可见变化

### 二、四卡从“数据展示”改成“控制器”
将下方四卡变为真正的控制模块，点击后驱动主舞台：
1. `220°C` → 温度模式（Heat）
2. `45m` → 速度模式（Rhythm）
3. `0 接触` → 自动化模式（Automation）
4. `24h` → 保鲜模式（Freshness）

规则：
- 同时仅允许一个模式激活
- 再点当前激活卡片可回到 Auto
- 激活卡必须有明显视觉反馈（边框/辉光/状态标签）
- 卡片状态变化必须同步到主舞台（不能只改卡片样式）

### 三、主舞台“烘焙反馈”增强（可稍牺牲流畅度换冲击）
在不更换整体风格前提下，显著增强以下反馈：
- 面包色泽变化更明显（浅金→金黄→焦香）
- 蒸汽粒子密度与上升速度增强
- 炉膛光晕脉冲更强
- 传送带振动与节奏变化更可感知
- 鼠标/触控操作带来的热场感（非液化，偏热浪/蒸汽）

要求：
- 可牺牲一点流畅度，但不得出现明显卡死或交互中断
- 视觉冲击感优先于“过分保守的性能降级”

### 四、“火候状态”文案改为舞台中心提示
将“松软 / 金黄 / 焦香”从右上角移动到 Factory 画面中部（或中下部安全区），作为主反馈文案：
- 字体：偏艺术化（与现有 serif 调性一致）
- 动效：缩放 + 轻虚化过渡 + 淡入淡出
- 文案状态随长按进度切换
- 松开后延迟淡出，不要瞬间消失

### 五、生命周期与恢复（重点修复）
必须满足：
- 滑到下一屏时，Factory 重渲染暂停（节约资源）
- 滑回 Factory 时，交互和动画**必须自动恢复**
- 连续来回滚动 5 次后仍可长按、可点击四卡、可见动画

请确保 rAF 生命周期、可见性监听、交互监听三者不会互相打断。

### 六、统一状态架构（避免再次“看起来改了但没生效”）
请把交互状态集中管理（例如一个 factoryState）：
- 当前模式
- 长按进度
- 速度系数
- 火候阶段
- 可见性/运行状态

所有视觉效果都从统一状态读取，不要分散在多个互相独立的定时器里。

### 七、验收标准（必须全部通过）
1. 长按 1 秒内：
   - 进度变化明显
   - 面包变色明显
   - 蒸汽与炉光增强明显
2. 四卡点击后：
   - 每张卡都触发不同舞台反馈
   - 再点可恢复 Auto
3. 滚出 Factory 再滚回：
   - 交互不中断，可继续操作
4. 控制台无报错
5. 不改变网站其他模块正常滚动与导航

### 八、输出要求
完成后请输出：
- 改动点清单
- 关键状态字段说明
- 5轮“滚出/滚回”恢复性测试结果
- 已知风险与建议（如需后续再做性能分档）

---

## 执行约束
- 只改 `index.html` 的必要代码，不做无关重构。
- 保持原品牌美术风格，不引入突兀霓虹或赛博风。
- 不要只做“样式假交互”，必须是真实状态驱动。
